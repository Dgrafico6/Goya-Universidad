<!-- MODAL / OVERLAY -->
<div id="simpleModal" class="overlay hidden">
  <div class="modal-box">
    <button class="close-btn" id="closeModalBtn">✖</button>
    <h2>Hola desde el overlay 🔵</h2>
    <p>Este es un modal azul básico.</p>
  </div>
</div>

<style>.hidden {
  display: none;
}

.overlay {
  position: fixed;
  top: 0; left: 0;
  width: 100vw;
  height: 100vh;
  background-color: rgba(0, 0, 0, 0.4);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 9999;
}

.modal-box {
  background-color: #007BFF; /* Azul */
  padding: 2rem;
  border-radius: 10px;
  color: white;
  position: relative;
  width: 90%;
  max-width: 400px;
  box-shadow: 0 10px 25px rgba(0,0,0,0.2);
  animation: fadeIn 0.3s ease-in-out;
}

.close-btn {
  position: absolute;
  top: 10px;
  right: 12px;
  background: transparent;
  border: none;
  font-size: 1.2rem;
  color: white;
  cursor: pointer;
}

@keyframes fadeIn {
  from { transform: scale(0.9); opacity: 0; }
  to { transform: scale(1); opacity: 1; }
}
</style>